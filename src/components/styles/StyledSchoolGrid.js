import styled from 'styled-components';

export const StyledSchoolGrid = styled.div`
    display: grid;
    grid-template-rows: repeat(3, 1fr);
    grid-gap: 15px
    justify-content: center;
    border-radius: 5%;
`